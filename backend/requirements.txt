# Runtime only. shap and pandas are NOT here: SHAP is computed in closed form for
# the linear model (see main.py), which keeps the deployed image inside a 512MB host.
# A test asserts the closed-form values match shap.LinearExplainer exactly.

fastapi==0.110.0
uvicorn==0.27.1
pydantic==2.6.3
python-dotenv==1.0.1

numpy==1.26.4
scikit-learn==1.4.1.post1
joblib==1.3.2

sqlalchemy==2.0.29
# Postgres driver. Only needed in production: locally DATABASE_URL is SQLite, which
# ships with Python, so a missing driver cannot surface until deploy.
psycopg2-binary==2.9.9
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
email-validator==2.1.1
